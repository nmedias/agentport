// Root of the DS `blocks` layer (@agentport/ui/blocks). Per-screen blocks live in
// nested subpaths (e.g. @agentport/ui/blocks/explorer). Cross-screen shared
// blocks could go here; empty for now.
export {};
